# ── Core ──
# Install torch / torchvision separately for your CUDA or CPU environment.
# torch>=2.1.0
# torchvision>=0.16.0
numpy>=1.24.0
Pillow>=9.0.0
einops>=0.7.0

# ── Model dependencies ──
diffusers>=0.27.0
transformers>=4.45.0
timm>=0.9.0
pyquaternion>=0.9.9

# ── Qwen3-VL (same attention path as VLA-JEPA QWen3.py) ──
qwen-vl-utils>=0.0.8
# pip install flash-attn --no-build-isolation  # required for attn_implementation=flash_attention_2

# ── Evaluation ──
omegaconf>=2.3.0
torchmetrics>=1.0.0
safetensors>=0.4.0

tqdm>=4.62.0
matplotlib>=3.3.0

# ── Data ──
opencv-python>=4.5.0

# ── NuPlan / NAVSIM (install separately) ──
# pip install nuplan-devkit
# cd navsim && pip install -e .
